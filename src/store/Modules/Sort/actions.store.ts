const actions = {
    // /*@ts-ignore*/
    // sortByPickedField({ commit }, {fieldName = "", sortType = ""}) {
    //     commit("SORT_BY_PICKED_FIELD", {fieldName, sortType});
    // }
}

export default actions;
