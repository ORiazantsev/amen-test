const mutations = {
    // /*@ts-ignore*/
    // SORT_BY_PICKED_FIELD(state, {fieldName, sortType}) {
    //     state.fieldName = fieldName,
    //     state.sortType = sortType
    //},
}

export default mutations;
